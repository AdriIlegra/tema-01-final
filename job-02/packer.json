{
  "builders": [
    {
      "type": "docker",
      "image": "ubuntu:latest"
    }
  ],

  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "apt-get update",
        "apt-get install -y some-package",
        "apt-get clean"
      ]
    }
  ]
}
